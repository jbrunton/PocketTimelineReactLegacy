/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {TimelinesStack} from './App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => TimelinesStack);
